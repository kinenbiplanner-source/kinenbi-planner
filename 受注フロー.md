# Anniv 受注フロー（SSOT）

最終更新: 2026-09-07

**受注からサービス提供までの仕組みの正。** 誰がどこを触るか迷ったらここを見る。
記事・メディアの話は `メディア方針/` 配下、事業の進捗は `進捗メモ.md`。

---

## 1. なぜ作り直したか

2026-09-07 まで、1件の申し込みが **Tally → Notion 2つ → Make → Gmail** の4サービスに散っていた。
問題は3つあった。

- 案件の状態を Notion で手更新するしかなく、LP側の計測（D1・GA4）と繋がっていない
- 「顧客管理DB（生ログ）」と「案件管理DB（進行管理）」が別物になっているが、実態は同じ1行の前半と後半
- **LINE を追加した人が誰の申し込みなのか分からない。** これが一番効いていた

Anniv 全体を Cloudflare スタックへ寄せる方針もあり、**Tally・Make・Notion の3つを anniv.gift の Worker に畳んだ**。
LINE と Stripe は残す。顧客との対話と決済そのものなので、置き換える意味がない。

---

## 2. 全体像

**入口では3つしか聞かない。** 詳しいアンケートは友だち追加のあと LINE から案内する。

```
LP（anniv.gift）／記事（/media/*）
  ↓ CTA「無料で相談する」
/apply … お名前・メール・LINE名 の3つだけ（30秒）   /contact（無料相談・軽い窓口）
  ↓ POST /api/apply（Turnstile ＋ レート制限 ＋ ハニーポット）
D1 cases に1行 ＋ 受付番号を採番（例 K7M2-4QXA）／ survey_at は空＝アンケート未回答
  ├─ 顧客へ受付確認メール（Resend）… 頼むのは友だち追加だけ。アンケートの導線は載せない
  ├─ 運営者へ通知（メール ＋ LINE push）
  └─ /thanks?c=K7M2-4QXA へ
        ↓ 顧客がやるのは「友だち追加」だけ
     LINE 公式アカウント
        ↓
     POST /api/line/webhook（署名検証）
        ├─ 顧客へ自動送信「アンケートにご協力ください → /survey」
        ├─ 申込時刻と友だち追加の時刻を突き合わせて候補を出す
        └─ 運営者へ通知「◯◯さんの申し込みの2分後に追加」
              ↓ /admin/cases で候補が選択済み。表示名を見てワンタップ確定
           cases.line_user_id に紐づけ
        ↓
/survey … 記念日・予算・パートナーの好み（必須は3つだけ。残りは分かる範囲）
  ↓ POST /api/survey（受付番号 or 申込時のメールで案件を特定）
cases を更新して survey_at を立てる → 運営者へ通知

  以降の対応は LINE のトークで人がやる（プラン提案・見積・納品）
  進行の記録は /admin/cases で更新する
  決済は Stripe Invoice を手で発行し、URL を LINE で送る
```

### なぜ2段階にしたか

15問を入口に置くと、**答え切る前に離脱する**。連絡が取れる状態を先に作ってしまえば、
あとは LINE で何度でも聞ける。だから入口は「お名前・メール・LINE名」の3つに削り、
残りは友だち追加のあとに回した。

`/survey` も必須は3つ（記念日の種類・日付・予算）だけにしてある。
**全部埋めさせるより「分かる範囲で送ってもらう」ほうが返ってくる**ので、
足りないところは LINE のトークで人が聞く。

`/admin/cases` の一覧に「アンケート」列があり、未回答は色付きで出る。案件詳細（`/admin/cases/<id>`）にも
「アンケート未回答」の帯が出て、案内用のURLがそこに表示される。連絡する前にそこを見て、
埋まっていなければ LINE で回答を促す。

> **未回答のまま放置される人は必ず出る。** 現状は一覧を見て気づく運用で、リマインドは自動化していない。
> 未回答が溜まるようなら、記念日が近い案件と同じ形で「受付から24時間以上たった未回答」のカードを足すか、
> Cron Trigger で LINE のリマインドを送る。まずは未回答率を見てから決める。

### 申し込みと LINE をどう結ぶか

この2つは別の場所で起きるので、何かで結ばないといけない。**顧客の手間を増やさない**ことを優先して、
次の3段構えにしてある。上から順に手間が少ない。

1. **時刻の突き合わせ（既定・顧客の手間ゼロ）**
   申し込みからサンクスページの緑のボタンを押して友だち追加するまでは数十秒しかかからない。
   友だち追加の直前（90分以内）に来た未紐づけの申し込みを候補として出し、
   `/admin/cases` の「案件に紐づける」に**あらかじめ選択済み**で並べる。
   運営者は LINE の表示名と見比べて「紐づける」を押すだけ。
   **自動では確定しない。** Instagram のプロフィールから申し込みと無関係に友だち追加する人がいて、
   そのとき窓の中にたまたま未紐づけの案件が1件あると他人に結びつく。
   取り違えると別人の記念日プランと個人情報を送ることになるので、最後は必ず人が見る。
   実装は `src/lib/cases.ts` の `rankLinkCandidates`。
   **先に友だち追加していた人（Instagram から来て後日申し込む／リピーター）も拾えるように、
   追加より後に来た申し込みも7日前まで候補に含める**（LINE は既存の友だちに follow を再送しないので、
   窓を「追加の直前」だけにすると、その人たちの案件が一覧の下に沈む）
2. **`LINE_OA_ID` を入れたときの1タップ送信**
   サンクスページのボタンが `line.me/R/oaMessage/@id/?受付番号 XXXX` になり、
   友だち追加とメッセージ送信がまとめて開く。顧客は送信ボタンを押すだけで、
   届いた時点で**自動で確定**する（人の確認が要らない）
3. **受付番号を手で送ってもらう（最後の砦）**
   1 も 2 も外したとき用。サンクスページと受付確認メールに番号を残してあるので、
   「担当から連絡が来ない」と言われたらこれを送ってもらう。番号を受け取れば即座に自動で紐づく。
   **ただし、既に別の LINE が紐づいている案件は自動では奪えない**（番号はスクショなどから漏れうるので、
   運営者に通知して手動の紐づけに回す。`src/pages/api/line/webhook.ts`）

**受付番号は 1 の運用でも消さない。** 問い合わせのときの言い当てに要るし、2・3 の受け皿でもある。
`0/O` `1/I/L` を除いた31文字から8桁（`XXXX-XXXX`）で採番する（`src/lib/cases.ts` の `CODE_ALPHABET`）。

> **もっと確実にしたくなったら LINE ログイン。** Messaging API と同じプロバイダーに
> LINE Login チャネルを作れば、フォームの送信時に userId を直接取れて突き合わせが要らなくなる。
> ただし OAuth のコールバックと state 管理が増え、PC からだと QR ログインで一段面倒になる。
> 申し込みが増えて 1 の取り違えが実際に起きてから考えれば足りる。

---

## 3. データの置き場所

すべて D1 `anniv`。スキーマは `schema.sql` が正。

| テーブル | 中身 |
|---|---|
| `cases` | 案件1件＝1行。**旧 Notion の2つのDBを畳んだもの。** 申込内容の列＋進行の列＋生の回答（`submission_json`） |
| `case_log` | 案件の履歴（受付・状態変更・メモ・メール・LINE送受信・紐づけ） |
| `line_users` | 友だち追加した人。`follow` で行を作り、`unfollow` で `unfollowed_at` を立てる（**行は消さない**。再追加で戻る） |
| `line_messages` | LINE の送受信の控え。**全会話を再現する意図はない**（対話の本体は LINE公式アカウントのチャット画面） |

- `cases.kind` … `apply`（申込フォーム）/ `consult`（無料相談）。**同じテーブルで扱う**
- `cases.status` … 10段階。定義は `src/lib/intake.ts` の `CASE_STATUSES`
- **`cases.survey_at` が null なら アンケート未回答**＝提案に必要な情報がまだ揃っていない
- **`submission_json` に生の回答をそのまま持つ。** 設問を増やしても過去の行が壊れない

回答の集計は `/admin/cases/insights`（予算・記念日の種類・交際期間・お任せ度・リードタイム・
興味の上位・自由記述）。SQLではなく1クエリで全件引いてJSで畳んでいる（`src/lib/cases.ts` の `tally` 系）。
**申し込みの回答は検索KWより実需に近い**ので、記事のテーマ選びとプレゼントの仕入れ判断の両方に使える。

---

## 4. 画面とURL

| URL | 何 | 認証 |
|---|---|---|
| `/apply` | 申込フォーム。**お名前・メール・LINE名の3つだけ** | なし |
| `/survey?c=<受付番号>` | 詳しいアンケート。`?c=` が無いときは申込時のメールで照合する | なし |
| `/contact` | 無料相談フォーム（軽い窓口） | なし |
| `/thanks?c=<受付番号>` | サンクス。受付番号の表示とLINE追加導線 | なし |
| `/admin/cases` | 案件一覧。状態別件数・記念日が近い案件・未紐づけのLINE友だち。**アンケートの回答有無は表の1列** | Access |
| `/admin/cases/<id>` | 案件詳細。申込内容・進行の更新・LINE履歴・返信・履歴 | Access |
| `/admin/cases/insights` | 回答の集計。予算・興味・リードタイムの分布と自由記述 | Access |

**管理画面は静的HTMLではない。** `/admin/*` はすべて `export const prerender = false` の
Astro ページで、リクエストのたびに Worker が D1 を読んで描く。だから申し込みが入れば
リロードするだけで出るし、画面から押した保存はその場で D1 に書き戻る。

対して `public/` にあるのは素のHTML5枚（`index` / `contact` / `links` / `privacy` / `tokutei`）。
**そのうち `contact.html` だけはフォームを持つが、送信先は `fetch('/api/apply')` で Worker**。
つまり「静的ファイルだから問い合わせを拾えない」ということはない——**受け口はどちらも同じ API**で、
静的なのは見た目の側だけ。

`ダッシュボード.html` と `計測ダッシュボード.html` は例外で、リポジトリ直下に置いたまま
Worker がビルド時に取り込んで `/dashboard` から配信している（`src/pages/dashboard.ts`）。
**ローカルでファイルを直接開くと、中の `/api/...` へのfetchが解決できず数字が出ない。**
数字を見るときは必ず `https://anniv.gift/dashboard` を開く。

API（`src/pages/api/`）：

| エンドポイント | 認証 | 用途 |
|---|---|---|
| `POST /api/apply` | **なし** | 入口フォームの受け口。Turnstile ＋ 同一IP 10分5回のレート制限 ＋ ハニーポット |
| `POST /api/survey` | **なし** | アンケートの受け口。受付番号かメールで案件を特定して更新する。Turnstile は掛けない |
| `GET /api/cases.csv` | Access | 回答の生データをCSVで書き出す（表計算で自分で切りたいとき） |
| `POST /api/line/webhook` | **なし** | LINE からの通知。`X-Line-Signature` の HMAC 検証が防御線 |
| `GET/PATCH/DELETE /api/cases/:id` | Access | 案件の取得・更新・削除 |
| `POST /api/cases/:id/note` | Access | メモを履歴に追加 |
| `POST /api/cases/:id/send` | Access | LINE push またはメール送信 |
| `POST/DELETE /api/cases/:id/line` | Access | LINE の手動紐づけ・解除 |

**認証不要の API は5本だけ**（`/api/pv` `/api/ev` ＋ 上の3本。`src/middleware.ts` の `PUBLIC_API`）。
ここを増やすときは、受け口側の検証を必ずセットで書く。

---

## 5. 環境変数（Worker の secret）

すべて `src/lib/config.ts` の `readVar()` 経由で読む。**未設定なら、その機能だけを飛ばして他は動く**設計。

| 名前 | 無いとどうなる | 取得元 |
|---|---|---|
| `RESEND_API_KEY` | 受付確認メールが飛ばない（案件は保存され、履歴に「未設定」と残る） | Resend |
| `MAIL_FROM` | `Anniv <noreply@anniv.gift>` が使われる | 任意 |
| `OWNER_EMAIL` | `kinenbi.planner@gmail.com` が使われる | 任意 |
| `LINE_CHANNEL_SECRET` | **Webhook が全部 401**（本番では素通しにしない） | LINE Developers |
| `LINE_CHANNEL_ACCESS_TOKEN` | 自動返信・push・プロフィール取得ができない | LINE Developers |
| `LINE_OA_ID` | サンクスページの「番号を自動入力して送る」リンクが出ない（普通の友だち追加リンクに落ちる） | LINE公式アカウント のベーシックID |
| `LINE_OWNER_USER_ID` | 新規受付の LINE 通知が来ない（メール通知だけになる） | 後述 |
| `TURNSTILE_SECRET_KEY` | bot 判定を飛ばす（**フォームは止めない**） | Cloudflare Turnstile |
| `TURNSTILE_SITE_KEY` | `/apply` にウィジェットを出さない | Cloudflare Turnstile |
| `LINE_ADD_URL` | `https://lin.ee/U4deTzi` が使われる（OAのURLを変えたときだけ設定する） | LINE公式アカウント |
| `SITE_URL` | `https://anniv.gift` が使われる | 任意 |

入れ方（1つずつ、値は対話で聞かれる）：

```bash
npx wrangler secret put RESEND_API_KEY
```

ローカル（`astro dev`）で使うときは `.dev.vars` に `KEY=値` の形で書く（gitignore 済み）。

> `wrangler.jsonc` に `keep_vars: true` を入れてある。これが無いと `wrangler deploy` のたびに
> ダッシュボードで入れた変数が消える。

---

## 6. 外部サービスの設定手順

### 6-1. Resend（受付確認メール）

1. https://resend.com にアクセスして Sign up。GitHub か Google でログインでよい
2. 左メニュー **Domains** → **Add Domain** → `anniv.gift` を入力 → Region は `Tokyo (ap-northeast-1)` を選ぶ
3. DNSレコードが3〜4件表示される（MX 1件・TXT 2〜3件）。**Cloudflare のダッシュボードに手で入れる**
   - Cloudflare → `anniv.gift` → **DNS** → **Record を追加**
   - Resend の表を1行ずつ写す。Type / Name / Content をそのまま
   - **Proxy status は必ず「DNS only」（灰色の雲）にする。** オレンジのままだと検証が通らない
   - `Name` が `send.anniv.gift` のように出ていたら、Cloudflare 側には `send` だけを入れる（末尾のドメインは自動で付く）
4. Resend に戻って **Verify DNS Records**。数分で `Verified` になる。ならなければ10分待って再実行
5. 左メニュー **API Keys** → **Create API Key** → 名前は `anniv-worker`、Permission は **Sending access**、Domain は `anniv.gift`
6. 表示されたキー（`re_` で始まる）をコピー。**この画面を閉じると二度と見られない**
7. ターミナルで登録：

```bash
npx wrangler secret put RESEND_API_KEY
```

無料枠は月3,000通・1日100通。この規模なら当分無料で足りる。

> Cloudflare 自身のメール送信機能を使いたくなったら、差し替えるのは `src/lib/mail.ts` の
> `sendMail()` 1関数だけで済むように切ってある。

### 6-2. LINE Messaging API（友だち追加の把握と自動紐づけ）

**LINE公式アカウントは開設済み**（友だち追加URL `https://lin.ee/U4deTzi`）。そこに API を有効化する。

1. https://manager.line.biz を開き、Anniv のアカウントを選ぶ
2. 右上 **設定** → 左メニュー **Messaging API** → **Messaging APIを利用する**
3. 「開発者情報」を聞かれる。名前とメールアドレスを入れる
4. プロバイダーを選ぶ画面が出る。無ければ **新規プロバイダー作成** で `Anniv` と入力
5. 完了すると **Channel ID** と **Channel secret** が表示される。`Channel secret` を控える
6. 同じ画面の左メニュー **設定 → 応答設定** を開く
   - **応答モード**：`チャット` のまま（人が手で返信する運用を続けるため）
   - **あいさつメッセージ**：`オン` のまま
   - **応答メッセージ**：`オフ`
   - **Webhook**：**`オン` にする** ← これが今回の肝
7. https://developers.line.biz を開く（LINEアカウントでログイン）。4で選んだプロバイダー → Anniv のチャネルを開く
8. **Messaging API設定** タブ：
   - **Webhook URL** に `https://anniv.gift/api/line/webhook` を入れて **更新**
   - **Webhookの利用** を `オン`
   - **検証** を押す → `成功` が出れば疎通OK
   - 下の方の **チャネルアクセストークン（長期）** の **発行** を押して、出てきた文字列を控える
9. ターミナルで2つ登録：

```bash
npx wrangler secret put LINE_CHANNEL_SECRET
```

```bash
npx wrangler secret put LINE_CHANNEL_ACCESS_TOKEN
```

10. ベーシックID（`@` で始まる。LINE公式アカウントManager の ホーム → 右上のアカウント名の下に出ている）も登録：

```bash
npx wrangler secret put LINE_OA_ID
```

**分岐**：8の「検証」で失敗する場合
- `https://anniv.gift/api/line/webhook` にまだデプロイが届いていない → `npm run deploy` してから再検証
- 401 が返る → `LINE_CHANNEL_SECRET` が未登録か値違い。もう一度 `wrangler secret put`
- 6の Webhook を `オン` にし忘れていると、検証は通るのにイベントが来ない

**分岐**：自動返信（受付番号を送ったときの確認メッセージ）が返ってこない
- 応答モードが `チャット` だと reply API が通らない環境がある。その場合は **応答モードを `Bot` に変え、応答メッセージを `オフ`** にする。手動チャットは LINE公式アカウントManager の「チャット」から引き続き使える

### 6-3. 運営者の LINE userId（新規受付を LINE で受け取る）

自分の LINE で Anniv の公式アカウントを友だち追加してから、**何かメッセージを1通送る**。
そのあと `/admin/cases` を開くと「未紐づけの友だち」に自分が出る。表示名を確認して、その userId を登録する。

userId を調べるには、Worker のログを見るのが早い：

```bash
npx wrangler tail --format pretty
```

もしくは D1 を直接引く：

```bash
npx wrangler d1 execute anniv --remote --command "SELECT user_id, display_name FROM line_users ORDER BY followed_at DESC LIMIT 5"
```

```bash
npx wrangler secret put LINE_OWNER_USER_ID
```

**これを入れると、新規申し込みが来た瞬間に自分の LINE に通知が飛ぶ。** メール通知より気づきやすい。

### 6-4. Turnstile（bot対策）

1. Cloudflare ダッシュボード → 左メニュー **Turnstile** → **Add widget**
2. Widget name は `anniv-apply`、Hostname に `anniv.gift` を追加。Widget Mode は **Managed**
3. **Site Key** と **Secret Key** が出る。両方控える
4. 登録：

```bash
npx wrangler secret put TURNSTILE_SITE_KEY
```

```bash
npx wrangler secret put TURNSTILE_SECRET_KEY
```

**未設定でもフォームは動く**（検証を飛ばす）。先にフォームの動作確認をして、後から入れてよい。
**サイトキーと秘密鍵は必ず両方セットで入れる。** 片方だけだと `/apply` が「認証に失敗」で詰まる。

> **Turnstile が掛かるのは `/apply` だけ。** `/contact` は `public/` の素の HTML でサイトキーを
> 埋め込めず、ウィジェットを置けない（＝トークンを送れない）。ここも必須にすると
> 秘密鍵を入れた瞬間に相談が全部弾かれるので、`/api/apply` は `kind=consult` を免除している。
> 相談側の防御はハニーポットとレート制限。相談スパムが実害になったら `/contact` を
> Astro ページに移してウィジェットを載せ、`src/pages/api/apply.ts` の `needsTurnstile` を外す。

### 6-5. Cloudflare Access（`/admin/cases` の保護）

`/admin` 配下のパスポリシーが既にあるので、**追加の作業は要らない**。
`anniv.gift/admin` の宛先はサブパスを含む（`/dashboard` で確認済み。`メディア方針/改良ロードマップ.md` の1番）。

---

## 7. 動かすまでの手順

### 7-1. 本番 D1 にテーブルを作る

```bash
npm run db:remote
```

`schema.sql` を丸ごと流す。全部 `CREATE TABLE IF NOT EXISTS` なので既存のテーブルには影響しない。

> **転送に失敗することがある**（"File contents did not upload successfully"）。
> そのときは `schema.sql` の「受注（案件）」以降だけを別ファイルにコピーして `--file` で流すか、
> `--command` で1テーブルずつ流す。

### 7-2. デプロイ

```bash
npm run deploy
```

**`git push` では本番は変わらない**（Workers Builds を繋いでいない）。

**デプロイのあと `/admin` の「全記事を再レンダリング」を必ず押す。**
記事本文のCTAは保存時にHTMLへ焼き込まれているので、デプロイだけでは
**公開中の記事が Tally のURLと古い「LINEで相談する」ボタンを出したまま**になる。
併記をやめた分のCSSも消してあるので、押さないと崩れて見える。

### 7-3. 自分でひととおり通す

1. `https://anniv.gift/apply` を開いて、テスト用のメールアドレスで送信する
2. `/thanks?c=XXXX-XXXX` に飛び、受付番号が出ることを確認
3. 受付確認メールが届くことを確認（迷惑メールフォルダも見る）
4. `/admin/cases` に行が出ることを確認
5. サンクスページのボタンから LINE を友だち追加する（**番号は送らない**）
6. `/admin/cases` の「LINE 友だち」に自分が出て、**さっきの申し込みが選択済み**になっていることを確認。
   「◯◯さんの申し込みの N 分後に追加」のヒントが出ていれば時刻の突き合わせが効いている
7. 「紐づける」を押して、`/admin/cases/<id>` で LINE が紐づくことを確認
7-b. `LINE_OA_ID` を入れたときは、5 のボタンで受付番号が入った状態のトークが開き、
   送信すると**確認の自動返信が来て人の操作なしで紐づく**ことも確認する
8. 詳細画面の「返信」から LINE に1通送ってみる
9. 進行の列（プレゼント・レストラン・入金）を更新して保存できることを確認
10. `/contact` からも1件送って、`kind=consult` で入ることを確認

### 7-4. 過去の案件を Notion から移す（あれば）

Notion の案件管理DBを CSV でエクスポート（`···` → **Export** → Format: `Markdown & CSV`）してから：

```bash
node --experimental-strip-types scripts/import-cases.ts "<書き出したCSV>" --dry-run
```

中身を確認してから `--dry-run` を外して本実行。同じ「メール＋記念日」の行は飛ばすので、2回流しても増えない。

---

## 8. Tally・Make・Notion を止める（動作確認が済んでから）

**並走させたまま先に作らない。** 上の 7-3 が全部通ってから、この順でやる。

### コード側（2026-09-07 に完了。もう Tally は呼ばれていない）

- ✅ **LP と記事の CTA 18箇所を `/apply` に差し替えた**（`public/index.html` 3・`CtaBox` / `Footer` / `Nav`・
  `MediaArticle.astro` 2・`markdown.ts` の `FORM_URL`・`admin/new.astro` の `FORM_URL`・
  `media/index.astro` 3・`category/[axis].astro` 3・`page/[n].astro`・`search.astro`）。
  同一サイト内になったので `target="_blank"` も外した
- ✅ **LP の計測スクリプトのセレクタを `a[href*="tally.so"]` → `a[href^="/apply"]` に変更**。
  label も `tally_form` → `apply_form`
- ✅ `メディア方針/計測設計.md` の4章・5章を更新
- ✅ ダッシュボードから Tally・Notion・Make のカードとコスト欄の記載を撤去

### 残り（外部サービス側。デプロイしてから触る）

**順番を守る。** デプロイ前に Tally を閉じると、本番のLPはまだ Tally を指しているので申し込みが全部落ちる。

1. `npm run deploy`
2. **本番で `/apply` から1件テスト送信して `/admin/cases` に入ることを確認**（ここまで通ってから次へ）
3. 1週間ほど様子を見て、申し込みが入り続けることを確認する
4. **Tally**：フォームを閉じる（**削除しない**。過去の回答が消える）。Integrations の Notion 連携2本をオフ
5. **Make**：シナリオを `Scheduling: OFF`。1か月動かさずに問題なければ削除
6. **Notion**：案件管理DB・顧客管理DB を CSV でエクスポートして手元に保存 → 7-4 で D1 に取り込んだらアーカイブ
7. **`public/privacy.html` の4章から Tally・Notion・Make の3行を消す**（自前側の記載は追記済み）
8. `進捗メモ.md` の「現在のシステム構成」と「アカウント一覧」を書き換える

---

## 9. スマホでの LINE 導線（利用の主戦場なので個別に書く）

申し込みはほぼスマホから来る。**「LINEに飛べない」が起きると受注が全部落ちる**ので、
`/thanks` と `/contact` の LINE ボタンには次の対策を入れてある。触るときは壊さないこと。

- **`target="_blank"` を付けない。** `lin.ee` も `line.me` も OS がアプリへ橋渡しする
  ユニバーサルリンクで、新しいタブを開かせると橋渡しに失敗して Web 版が出たり無反応になる端末がある
- **ボタンを押した瞬間に受付番号をクリップボードへコピーする。** アプリに切り替わるとこの画面には
  戻ってこないので、番号を控えないまま行くと詰む。コピーは `execCommand` の**同期版を先に**撃つ
  （`navigator.clipboard` は非同期で、遷移が先に始まると書き込みが間に合わないことがある）。
  `preventDefault` はしない——遷移自体はブラウザに任せる
- **アプリ内ブラウザを検知して案内を出す。** Instagram・X・Facebook・TikTok のアプリ内ブラウザは
  LINE のユニバーサルリンクを握りつぶすことがある。Anniv の流入はSNS中心なので当たりやすい。
  UA を見て「⋯ から『ブラウザで開く』を選んでから、もう一度」を出す（LINE 自身のブラウザは除外）
- **`LINE_OA_ID` を入れると1タップになる。** `line.me/R/oaMessage/@id/?受付番号 XXXX` で
  友だち追加とメッセージ送信をまとめて開き、届いた時点で自動確定する。
  未設定なら普通の友だち追加リンクに落ち、時刻の突き合わせ（2章）で拾う。**どちらでも成立する**
- **画面では番号の送信を求めない。** 「追加するだけで大丈夫です」と書いてある。
  番号は控えとして出しておき、連絡が来ないときの逃げ道としてだけ案内する
- ボタンは 54px 高・LINE 緑（#06C755）。375px 幅で横スクロールが出ないことを確認済み

---

## 10. ハマりどころ

- **`/thanks` は Astro のページになった。** `public/thanks.html` を復活させると静的アセットが先に返って受付番号が出なくなる（Cloudflare アダプタはアセットを先に配信する）
- **LINE の Webhook は何があっても 200 を返す。** 200 以外だと LINE が再送し続けるため。処理の失敗は `wrangler tail` で見る
- **受付番号の照合は大文字小文字と全角を吸収する**が、`0/O` `1/I/L` は番号に使っていないので、それらを含む文字列は番号として扱わない（`src/lib/cases.ts` の `extractCode`）
- **メール送信の失敗で受付は失敗にしない。** 案件は先に D1 に入っていて、履歴に失敗が残る。運営者通知が来なかった日は `/admin/cases` を直接見る
- **Workers 無料プランのサブリクエスト上限は50。** Webhook は1リクエストで先頭20イベントまでに絞ってある
- **D1 に本物のトランザクションは無い。** 複数テーブルへの書き込みが途中で落ちうる前提で読む
- `curl` で `/api/*` を叩くと403（Astro の CSRF 保護）。`-H "Origin: https://anniv.gift"` を付ける。PowerShell では `curl.exe`

---

## 11. 関連ファイル

- フォームの設問と検証：`src/lib/intake.ts`（**設問を増やすときはここだけ直す**）
- 案件のDBアクセス：`src/lib/cases.ts`／ LINE：`src/lib/line.ts`・`src/lib/line-api.ts`
- メール文面：`src/lib/mail.ts`／ 返信の定型文：`src/lib/templates.ts`
- 取り込みスクリプト：`scripts/import-cases.ts`
- 計測の定義：`メディア方針/計測設計.md`
- 事業の進捗：`進捗メモ.md`／ サービス仕様：`仕様書.html`
