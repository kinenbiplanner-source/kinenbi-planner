# Anniv メディア 改良ロードマップ

最終更新: 2026-08-28

**このファイルは「これから何をするか」だけを書く。** 終わったら消すか「済」に倒す。仕様の説明は各SSOT（`メディア戦略.md` / `計測設計.md` / `article-style-guide.md`）に書き、ここには重複させない。

---

## いま詰まっていること

### www が無限リダイレクトになっている
下の「4. www → apex の Redirect Rule」を参照。**本番の不具合なので最優先**。

---

## Git 運用のメモ

`master` が本流でリモートのデフォルトブランチ（2026-08-26 に `main` から変更。`main` はGitHubのWeb UIから `index.html` を1つアップしたときにできた履歴の繋がらない孤立ブランチだったので削除した）。

push は Git Credential Manager がGUI認証を出すので、**手元のターミナルで実行する必要がある**（自動実行では認証待ちで止まる）。

作業ブランチを master へ寄せるときは、**checkout せずにリモート側を進める**のが安全（別セッションの未コミット変更があると `git checkout master` が弾かれるため）：

```bash
git push origin feat/astro-media:master
```

---

## すぐやる（ダッシュボード操作）

Cloudflare / Google の管理画面での作業で、CLIからは代行できない。

### 1. Access に `/dashboard` を追加　済（2026-08-26）
Access アプリの宛先に `anniv.gift/dashboard` を追加済み。未ログインで開くと Access のログイン画面へ302で飛ぶことを確認した（追加前は、ログイン画面へ飛ばされないまま `middleware.ts` に到達して403になっていた）。

> **2026-08-28 に `/dashboard/measurement` が増えたが、追加設定は不要だった。** 本番の未ログインアクセスで Access のログイン画面へ302（`redirect_url=/dashboard/measurement`）になることを確認済み＝**宛先 `anniv.gift/dashboard` はサブパスも含む**。今後 `/dashboard/` 配下にページを足すときも Access 側の作業は要らない。

### 2. Search Console に sitemap を登録し直す　済（2026-08-26）
`sitemap.xml` を送信し、トップと `/media` の再クロールもリクエスト済み。

**記事0本のいま、検出されるURLは4件（`/`・`/privacy`・`/tokutei`・`/media`）で正常。** `sitemap.xml` は毎回D1を読むので、記事を公開すれば自動で増える。**再送信は不要**。

### 3. GA4 のキーイベント設定　済（2026-08-26）
`form_complete` と `line_add_click` をキーイベントとしてマーク済み。あわせて `follow_click` の着弾も本番のリアルタイムレポートで確認した。詳細は `計測設計.md` 6章。

### 4. www → apex の Redirect Rule　**ルールは作ったが壊れている。要修正**
2026-08-26 にルールを作成したが、**リダイレクトの「種類」が静的（Static）のまま**で、式が評価されず文字列としてそのままURLに入っている。結果 `https://www.anniv.gift/concat("https://anniv.gift", ...)` へ飛び、**www 全体が無限リダイレクトループ**になっている（ルール作成前は200を返していたので、状態としては悪化）。

Rules → Redirect Rules → 該当ルールを編集
- If: Custom filter expression → Hostname **equals** `www.anniv.gift`
- Then: **種類を「動的（Dynamic）」に変える**（ここが肝）／式 `concat("https://anniv.gift", http.request.uri.path)` ／ **301** ／ Preserve query string にチェック

> **静的（Static）だと入力値がそのままリダイレクト先の文字列になる。** パスを引き継ぐには動的（Dynamic）が必須。すぐ直せないときは、ひとまずルールを**無効化**してループを止める。

> Worker側では直せない。Astro の Cloudflare アダプタが**静的アセットを先に返してから Astro 本体を呼ぶ**ため、`/` や `/contact` には middleware が効かない（`run_worker_first` でも解決しなかった）。

### 5. Tally のリダイレクト先を確認　済（2026-08-26）
対象フォームは `https://tally.so/r/lbVDPB`。送信後のリダイレクト先を拡張子なしの `https://anniv.gift/thanks` に揃えた。

### 6. Vercel の後片付け　済（2026-08-26）
Vercel のプロジェクト、ワイルドカード `*` Aレコード2件、`_domainconnect` CNAME をすべて削除。リポジトリの `vercel.json` も消した（`cleanUrls` 相当は `wrangler.jsonc` の `html_handling` が引き継いでいる）。

存在しないサブドメイン（例 `zzztest123.anniv.gift`）が NXDOMAIN になり、apex が Cloudflare のIPを返し続けていることを確認済み。

---

## 記事を書く（最優先。他は全部その次）

基盤は完成していて、キーワードも59件がD1に入っている（`/admin/keywords`）。`priority=1` の21件から着手する。

着手順は**軸1（gift）→ 軸2（date）→ 軸3（concierge）**（メディア戦略.md 7章）。ただし**軸3はSERPがほぼ空白**（「プレゼント選び 代行 サービス」は個人向け解説が1件も存在しない）ので、軸1が3〜4本たまったら早めに取りにいく価値がある。

**勝てない領域**（調査で確定済み。ここに時間を使わない）：
- 「プレゼント おすすめ／ランキング／相場」系 → TANP・ギフトモール・伊勢丹が自社調査データで独占
- 「記念日ディナー 個室／夜景 ＋エリア」系 → OZmall・一休の予約DBが独占。**予約在庫を持たない立場では構造的に無理**

**最大の競合**：`anny.gift`（ギフトモール系）。狙い目のロングテールにガイド記事を量産中で内容も良い。正面衝突するKWは `keywords` テーブルの note に差別化方針を書いてある。

---

## 改良（効果が大きい順）

### A. アイキャッチ画像の運用を決める
R2アップロードは実装済み。**一覧のCTRに直接効く**ので記事が数本たまる前にやる。

> 繋ぎとして、アイキャッチ未設定のカードは**軸で地の色・記事idでグラデーションの角度**を変えるようにした（`media.css` の `.card-thumb.is-empty.is-*`）。全カードが同じ絵になる問題は消えたが、**実画像の代わりにはならない**。テンプレが決まったらこのCSSは消せる。

**残っている作業**：下のプロンプトで軸別に3枚のベース画像を作る → 1本の記事で試す → 良ければ `素材/` に置いて運用開始（＋上記CSSを消す）。

#### 決めたこと（テンプレ）

- **1200×675（16:9）**。カードは `object-fit: cover` の16:9、かつ**記事のOGP画像を兼ねる**（`MediaArticle.astro` の `ogImage={heroPath}`）。X等で 1.91:1 に切られるので**上下左右4%はセーフマージン**として何も置かない
- **ベース1枚＋記事ごとの差分**という作り。差分は**右のアーチの中のイラスト1点**だけに集約し、左のテキストとアーチの位置は動かさない（動かすと一覧がバラつく）
- **テキストは左55%に2行まで**。1200px基準で**60px以上**（カードは実寸360px前後まで縮むので、これ以下だと一覧で潰れる）
- **焼き込む文字は記事タイトルの丸写しにしない**。カードの下にHTMLでタイトルが出るので重複する。入れるなら「予算1万円台」「個室の取り方」のような**短いフック語だけ**（A-2 の「文字は焼き込まない」と同じ理由で、長い文言を焼くと直すたびに作り直しになる）
- 書き出しは PNG → `node .claude/scripts/card-renderer/to_webp.mjs <png>` で WebP。アップロードは記事エディタの「ヒーロー画像」欄

#### ベース画像の生成プロンプト

構図は**アーチ窓**。左の濃紺パネルにテキスト、右のアーチの中に記事ごとのイラストを置く。
一覧は実寸360px前後まで縮むので、**強い明暗差＋大きな図形1つ＋広い余白**で作る（細かい装飾は消える）。

```
An editorial key visual for a premium Japanese anniversary media, 16:9 landscape.
Designed so that text can be placed on the left panel and one small illustration
can be placed inside the arch on the right.

Composition: a confident asymmetric split. The left 58% is a deep navy (#1a2840)
matte panel, almost empty, with a barely visible paper texture. A single hairline
rule in muted gold (#c4a35a) runs horizontally across the lower third, edge to
edge, passing behind the arch. The right side holds one tall rounded arch window
(a portal shape, cropped by the right edge), filled with a smooth vertical
gradient from pale blue (#d6eaf8) at the top to ivory (#fafaf8) at the bottom,
outlined by a thin muted gold line with a soft gold-foil grain.

Details: an ultra-thin second arch line offset just outside the first, a small
solid gold dot where the hairline meets the arch, and a very faint radial glow
behind the arch. Matte finish, fine film grain, deep blacks, no gloss.

Style: high-end print design, art-deco influenced editorial graphic, flat vector
shapes with subtle grain, strong contrast, generous negative space.
No text, no letters, no numbers, no logo, no watermark, no people, no faces,
no hands, no gift boxes, no sparkles, no bokeh, no 3D render, no photorealism.
```

Midjourney なら末尾に `--ar 16:9 --style raw --v 7`、ネガティブに `--no text, letters, watermark, people, faces, sparkles, photorealistic, 3d render`。

**軸ごとの色違い**：構図・ゴールドの線は3軸で共通にして、**アーチ内のグラデ2色だけ**を変える。左のパネルは全軸ネイビー固定（白文字の可読性をここで担保する）。

| 軸 | アーチ内のグラデ | 印象 |
|---|---|---|
| ギフト（gift） | `#fafaf8` → `#ddc488` | 暖色。金の光 |
| デート（date） | `#f0f7fc` → `#a8c8e0` | 寒色。淡い青 |
| 代行（concierge） | `#f0f7fc` → `#4a8ab5` | 濃い青。落ち着き |

**別案**（アーチが硬く見えたとき）：45°の対角スプリットで左下ネイビー・右上アイボリー、右上の角から特大のゴールドの円を半分だけ画面に入れる。より図形的で小サイズに強い。テキストはアイボリー側に置く。

> **フラットなベクター調で出すこと**。A-2 のCTA画像は生成だと暗くなりすぎて `素材/` のCC0写真に差し替えている。写真調を狙うとここも同じ轍を踏むので、写真を敷きたい場合は生成ではなく `素材/`（63枚のCC0）から選ぶ。

### A-2. CTA画像を2枚置く　済（2026-08-28）
本文中のビジュアルCTA（`:::hero`）と右レールのCTAカードに写真を敷いた。**生成画像ではなく `素材/` のCC0写真**を使っている（生成すると暗くなりすぎた）。

- `public/assets/cta-hero.webp` … 3:1（1800×600）。`素材/restaurant-bar-glasses-12.webp` の中央クロップ。**暗いレストラン＋琥珀色の灯り**で、ネイビーのグラデが乗ると紺×ゴールドになる
- `public/assets/cta-square.webp` … 1:1（600×600）。`素材/gift-box-gold-ribbon-06.webp` の中央クロップ。**黒地×ゴールドのリボン**で、紺のカードの上にブランド色がそのまま続く
- **横長＝場（レストラン）／正方形＝物（プレゼント）**で被らせない。差し替えるときも被写体の種類は分けること
- **文字は焼き込まない**（見出し・補足・ボタンはHTML側が出す）。焼き込むとモバイルで潰れ、文言を変えるたびに画像を作り直すことになる
- 差し替えは `sharp` で `fit:'cover'` にかけるだけ（比率だけ合わせる）

### B-2. 計測をダッシュボードに集約　済（2026-08-28）
自前計測（`/api/ev` → `event_daily`）とGA4 Data API（`/api/ga4`）を集約した。**置き場所は 2026-08-28 に `/dashboard/measurement` へ分離**（`/dashboard` 内だと縦に伸びて固定費とサービスの入口が埋もれたため。1280幅で `/dashboard` は 2,744px → 2,277px に縮んだ）。`/dashboard` と `/admin` のヘッダー「計測」から飛べる。
UTM規約・実装・GA4のサービスアカウント設定はすべて `計測設計.md` 7・8・11章にある。

- **流入元別に「クリック総数／うちCV」が並ぶ**ので、来ているのにCVしない流入元がすぐ分かる
- 自前とGA4は**列を分けて出す**（必ずズレるので足さない・突き合わせない）
- **残っているのは「どの記事から相談に来たか」**。今は `utm_source` までしか分からない。
  記事内のCTAリンクに `utm_campaign=<slug>` を足すのが最小の追加（`計測設計.md` 10章）
- **SNS側の指標（フォロワー推移・投稿別）の合流は未着手**。`SNS_DB` バインディングで
  読める経路はあるが、**multi-SNS-manager 側の Instagram インサイトが実API未検証**なので待ち

### B. PVの推移グラフ　済（2026-08-26）
`/admin/stats`（管理画面ヘッダーの「メディア分析」）として実装。日次PVの棒＋7日移動平均（30日/90日切替）、記事別の直近28日 vs 前28日、軸別カバレッジ、月別の公開ペース。

- リライト候補は自動で印が付く（**下降**＝直近28日が前28日の70%未満／**伸び悩み**＝公開45日超で直近28日が20PV未満）。しきい値は `src/lib/stats.ts` の定数。立ち上げ期の暫定値なので、記事が20本を超えたら実データを見て決め直す
- 計測開始から28日たつまでは判定を出さない（古い記事が「PVが無い＝伸び悩み」に見えるため）
- ここのPVは `/api/pv` のビーコンで数えた自前の値。GA4とは必ずズレるので、突き合わせない

### C. 下書きのプレビューURL
今は公開しないと本番の見た目を確認できない。`?preview=<トークン>` で下書きを本番レンダリングで見られるようにすると、公開前チェックが確実になる。

### D. 内部リンクの自動提案
今は anniv-write-article が `KWマスターDB.csv` を見て手動で候補を出している。記事が10本を超えたあたりから、**D1の記事から関連度の高いものを自動提案する**仕組みが効いてくる。

> エディタ側は先に入れた：ツールバーの「内部」から公開済み記事を検索して、style-guide 11章の文型（段落を分けた誘導文＋アンカーは記事タイトル）ごと差し込める。本文中の `/media/<slug>` が実在するかも公開前チェックで見る。**残っているのは「どの記事を貼るべきか」の提案**（軸・KWの近さから候補を出す部分）。

### E. 使われなくなったCSSの掃除
メディアUI刷新で `article.css` の一部（`.article-layout` `.article-side` `.toc-card` `.badge` など）が未使用になった。記事レイアウトは `media.css` の `.art-*` に移っている。動作に影響はないが、放置すると次に触る人が混乱する。**削除前に必ず使用箇所を精査すること**（クラス名が部分一致するので単純grepでは判定を誤る）。

### F. 一覧系ページの構造化データと薄いページ対策　済（2026-08-26）
メディアTOP＝`Blog`＋`BreadcrumbList`、カテゴリ＝`CollectionPage`＋`BreadcrumbList`、ページ送り＝`BreadcrumbList`。記事（`Article`＋`BreadcrumbList`）と合わせて全ての面が揃った。パンくずの表示も4種類すべてに入れてある。

- **記事0本のカテゴリは `noindex`**。中身が「準備中」だけのページを検索結果に出しても評価が下がるだけなので、記事が入るまで伏せる。`sitemap.xml` 側も同じ条件（公開記事のある軸だけ）で出し分ける
- 記事が1本でも入れば自動で index に戻る。**手で戻す作業は不要**

### G. AIクローラ設定の再判断（3〜6か月後）
2026-08-26 に Cloudflare の「管理された robots.txt」をオフにして、AI検索に拾わせる方針にした。**実際にAI経由の流入が出るか、逆に引用されず取られるだけかはデータを見てから判断し直す**。

### H. AdSense の審査を通す　**受け皿は実装済み。審査は記事を公開してから**
2026-08-28 に「AdSense はやる」と決めて、コード側の受け皿を入れた（`src/lib/ads.ts` / `AdSlot.astro` / `MediaArticle.astro` / `media.css`）。プライバシーポリシー8章の追記も済み。
**`src/lib/ads.ts` の `ADSENSE_CLIENT` が空のあいだは、スクリプトも枠も1つも出力されない**ので、このまま置いておいて害はない。

残っているのはダッシュボード側の作業だけ。手順の全文は `収益化メモ.md` 3章。

1. **記事を3〜5本公開する（これが最大の関門）。** 記事0本のまま申請しても落ちる
2. AdSense に登録 → `https://anniv.gift` を追加
3. 所有権確認は **ads.txt スニペット方式**を選ぶ（メタタグ／コードスニペット方式だとLPの素のHTMLに広告用JSを貼ることになるので選ばない）→ `public/ads.txt` を作ってデプロイ
4. 審査申請（数日〜2週間）
5. 通ったら広告ユニットを2つ作る（記事末尾＝レスポンシブ／左サイド＝160×600）
6. `src/lib/ads.ts` にIDを入れてデプロイ
7. PageSpeed Insights で記事ページのCLSを測り直す

> **自動広告（Auto ads）はオンにしない。** 挿入位置をGoogleに任せるとCLSと本文の読みやすさが制御できなくなる。枠は `ads.ts` で定義した2つだけ。

### I. アフィリエイトの判断
`収益化メモ.md` 4章に候補と前提を整理済み。**AdSense とは独立に、記事数が積み上がるまで判断しない**。

---

## 関連ファイル
- 戦略: `メディア方針/メディア戦略.md`
- 計測: `メディア方針/計測設計.md`
- 収益化: `メディア方針/収益化メモ.md`
- 記法: `.claude/agents/reference/article-style-guide.md` 9章
- KW: `記事管理/keywords-research.json`（調査の生データ）／ `/admin/keywords`（運用）
