# Anniv メディア 収益化メモ（検討用）

最終更新: 2026-09-10

**AdSense は「やる」と決めた（2026-08-28）。アフィリエイトは未定のまま。**
AdSense の受け皿（広告枠・プライバシーポリシー）は実装済みで、あとは審査を通してIDを入れるだけ。
残りの手順は 3章に全部書いてある。アフィリエイトは 4章のまま検討材料の置き場として残す。

---

## 1. 現状

- **収益導線は Anniv 本体への送客（無料相談）だけ。** 記事のCTAは `.claude/agents/reference/content-axis.md` の軸別CTAブロックで、着地点は自前の申込フォーム `/apply`（`受注フロー.md`）
- **AdSense は受け皿だけ実装済み・配信はまだ0。** `src/lib/ads.ts` の `ADSENSE_CLIENT` が空のあいだは、スクリプトも枠も一切出力されない。審査が通ってIDを入れた瞬間に出はじめる
- **アフィリエイトは1本も入っていない。** 記事に外部の収益リンクは存在しない
- `記事管理/アフィリ案件マスター.csv` はヘッダーだけの雛形が置いてある（`案件名,提携状況,トラッキングURL,バナーHTML,代表報酬,備考`）。中身は空
- `.claude/agents/reference/article-style-guide.md` の19章に、アフィリエイトリンクと広告表記のルールが**先に**書いてある（案件マスターCSVを一次ソースにする・トラッキングURLを改変しない・PR表記を必ず出す・frontmatter に `ad: true` を持たせる、など）。記事DB側も `articles.is_ad` を持っていて、CSVエクスポートの備考欄に「PR記事」と出るところまで通っている
- つまり**仕組みは先に用意してあり、案件データと判断だけが無い**状態

## 2. 判断の前提（どちらを選ぶにせよ共通）

- SEOはCVへの主力ではなく、SNSと並ぶチャネルの一つという位置づけ（メディア戦略.md 4章）。**記事の主目的は本体サービスへの送客**であって、広告収益ではない
- 立ち上げ期でPVが小さいうちは、**広告収入よりCV1件の方が桁違いに大きい**。広告を入れて記事の信頼感やCTAのクリックが落ちるなら、本業のマイナスの方が確実に効く
- 逆に「読者が最終的に外部で予約・購入する」タイプの記事（レストラン予約・ギフトEC）は、送客しても Anniv の売上にならないまま離脱している。**そこだけを収益化する**という考え方はあり得る

## 3. Google AdSense（導入する）

### 決めたこと

| 項目 | 決定 |
|---|---|
| 出す場所 | **メディアトップ（`/media`）と記事ページ（`/media/<slug>`）だけ。** LP（`public/*.html`）・カテゴリ・ページ送り・検索・管理画面には出さない |
| 自動広告 | **使わない。** Google に挿入位置を任せるとCLSと本文の読みやすさを制御できない |
| 枠の数 | 1ページにつき2つ（下の表）。合計4枠 |
| CTAとの順番 | **CTAが常に上。** 広告でCTAを押し下げない |

### 枠の一覧（AdSenseで作る広告ユニットと1対1）

| `ads.ts` のキー | 場所 | サイズ | AdSenseで作るユニット |
|---|---|---|---|
| `articleBottom` | 記事ページ末尾（CTAの下） | レスポンシブ | ディスプレイ広告／レスポンシブ |
| `articleSide` | 記事ページ左の縦長（1440px以上でだけ出る） | 160×600 固定 | ディスプレイ広告／固定 160×600 |
| `listBottom` | メディアトップ最下部（CTAの下） | レスポンシブ | ディスプレイ広告／レスポンシブ |
| `listSide` | メディアトップ右サイドバー（CTAカードの下） | 300×250 固定 | ディスプレイ広告／固定 300×250 |

**サイドの2枠を固定サイズにしているのは追従サイドバーのため。** レスポンシブ枠は幅から高さを
計算するので、300pxのカラムに置くと 300×600 まで伸びる。すると追従サイドバーが画面より高くなり、
下半分が見えないまま固定される＝広告がビューアブルにならないうえCTAも押し出される。

理由は 2章のとおりで、**立ち上げ期はCV1件のほうが広告収益より桁違いに大きい**。
広告は「読者が最後まで読んで、それでも相談しなかった人」からだけ拾う置き方にしてある。

### 実装（済み）

| ファイル | 役割 |
|---|---|
| `src/lib/ads.ts` | **SSOT。** パブリッシャーIDとスロットIDはここだけ。空＝広告を一切出さない |
| `src/components/AdSlot.astro` | 広告枠。「広告」ラベル付き。未設定なら `<ins>` も `<script>` も出さない |
| `src/layouts/MediaLayout.astro` | `adsbygoogle.js` の読み込み。**`ads` を渡したページだけ**（＝枠のある2ページ） |
| `src/layouts/MediaArticle.astro` | 記事ページの枠の設置 |
| `src/pages/media/index.astro` | メディアトップの枠の設置 |
| `src/styles/media.css` | `.ad-inline` / `.ad-card` / `.ad-side` の `min-height`（CLS対策） |
| `public/privacy.html` | 8章に「第三者配信の広告について」を追記済み（2026-08-28） |

### 審査に1回落ちている（2026-09-10）

**不承認の理由＝「有用性の低いコンテンツ」。** サイトを実際に測ったうえでの状況：

| | 状態 |
|---|---|
| 公開記事 | 14本（本文4,100〜6,900字／総量7万字弱） |
| アイキャッチ・description | 全記事あり。未差し替えの画像プレースホルダ0 |
| 運営者情報ページ | **無かった**（`/about` `/company` すべて404） |
| 記事の著者表示 | **無かった**。構造化データも `Organization: Anniv` だけ |
| 記事本文中の内部リンク | **0**（関連記事枠と前後ナビのみ） |
| 公開ペース | 14本すべて 8/28〜9/5 の9日間 |

**記事の量は足りていた。** 欠けていたのは「誰がどういう体制で作っているか」を示す面（E-E-A-T）だったと判断し、
2026-09-10 に以下を入れた。個人のプロフィールは出さず、**運営主体は事業者としての Anniv、
記事の名義は「Anniv編集部」**で通す方針（`/tokutei` が所在地と代表者名を持つ）。

| ファイル | 内容 |
|---|---|
| `src/pages/about.astro` | **新規。`/about`「このメディアについて」。** 運営者・扱うテーマ・**編集方針5項目**・広告について・問い合わせ |
| `src/components/AuthorBox.astro` | **新規。** 記事末尾の「この記事を書いた人」（本文とCTAの間） |
| `src/layouts/MediaArticle.astro` | AuthorBox の設置＋構造化データの `author` を `Anniv編集部` / `url: /about` に |
| `src/components/Footer.astro`・`public/index.html`・`public/contact.html` | フッターに `/about` |
| `src/pages/sitemap.xml.ts` | `STATIC_PATHS` に `/about` |

> **`/about` に書くのは実際に運用しているルールだけ。** 守っていない方針を並べると記事本文と食い違って逆効果になる。
> 出どころは `メディア戦略.md`・`article-style-guide.md`（自己言及の禁止・料金の書き方）・`interview-sheet.md`（公開前の取材）。
> **運用を変えたら `/about` も直す。**

上の表の「記事本文中の内部リンク 0」は1本だけ見て全体に当てはめた誤りで、実際は21本あった。問題は**新しい記事への被リンクが無い孤立記事が8本**あったこと。**2026-09-13 に17本足して孤立0にした**（21 → 38本。位置は各記事の関係するセクション末尾、型は style-guide 11章）。

### 残っている手順

1. **記事を3〜5本公開する。** 記事0本のまま審査に出しても「価値の高いコンテンツがない」で落ちるだけ（公式の本数基準は無いが、実質ここが最大の関門）
2. AdSense に登録し、`https://anniv.gift` を追加する
   - **支払いプロファイルの住所は「郵便が確実に届く住所」にする。特商法の住所と揃えなくていい。**
     `tokutei.html` に載せている大阪駅前第2ビルはバーチャルオフィスで、普通郵便のハガキが
     受け取れない可能性がある。AdSense の支払い先住所は**非公開**（サイトにも広告にも出ない）ので、
     特商法表示と食い違っていて問題ない
   - 収益が約1,000円（10ドル相当）に達すると、この住所に**PINのハガキが自動で郵送される**。
     再発行は合計3回まで。届かないまま4か月経つと身分証のアップロードで代替確認できる。
     **住所を後から直すと再発行を1回消費するので、登録時に確定させる**
3. 所有権の確認は **ads.txt スニペット方式**を選ぶ。`public/ads.txt` を作ってデプロイする（**メタタグ方式やコードスニペット方式だと、LP側の素のHTMLに広告用のJSを貼ることになる**ので選ばない）
4. 審査を申請する（数日〜2週間）。**2回目以降は「問題を修正しました」にチェックしてから「審査をリクエスト」**。直後に出し直すと同じ判定が返りやすいので、修正から2週間ほど置く
5. 通ったら AdSense で広告ユニットを4つ作る（上の「枠の一覧」の右端の列のとおり）
6. `src/lib/ads.ts` の `ADSENSE_CLIENT` と `AD_SLOTS` の4つを埋めてデプロイ
7. **PageSpeed Insights で記事ページのCLSを測り直す。** 悪化していたら枠の `min-height` を実測値に合わせる

### 入れた後に見ておくもの

- **CLS（Core Web Vitals）**。`<ins>` の高さを決めるのは `adsbygoogle.js` で、広告が届く前に走る。予約が足りないとそこでレイアウトが動く。**実測で合わせてある**（レスポンシブ枠は 375px幅→375px高／760px幅→280px高。だから `.ad-inline` は 768px を境に予約する高さを変えている）。**サイズの前提を変えたら `media.css` の `min-height` も必ず直す**
  - 広告が返らなかったときは `<ins>` に `data-ad-status="unfilled"` が付く。ラベルだけ隠して**高さはそのまま残す**（畳むとそこで動いてCLSになる）
- **CTAのクリック率**（`cta_click` / `line_add_click`）。広告を入れる前後で落ちていないか。落ちたら枠を減らす。広告収益よりCVが優先という順番は変えない
- 単価。ジャンル的にディスプレイ広告のRPMは高くない。**PVが数万/月に届くまでは金額として意味を持たない**前提で考える

### まだやっていない（PVが出てから判断する）

- **本文中の広告**（H2の直前など）。モバイルで一番効く位置だが、本文HTMLを分割して挿し込むことになる。`markdown.ts` が出す `:::box` / `:::faq` の入れ子を跨いで割るとHTMLが壊れるので、**やるなら保存時のレンダラー側で区切り位置を印付けする**。PVがほぼ0の今やる価値はない
- 記事一覧・カテゴリページへの広告。回遊を削る側なので当面入れない

## 4. アフィリエイトを検討する場合

### 候補

- **レストラン予約サイト**（一休.com・OZmall・ヒトサラなど）。軸2（記念日デート・レストラン）の記事と読者の行動が完全に一致する。ASP経由で提携できる案件があるかは未調査
- **ギフトEC**（TANP・ギフトモール系など）。軸1（ギフト・サプライズ）と一致するが、SERPで既にこれらが上位を占めているため、**同じ商品を紹介する記事で勝つ**構図になる。難易度は高い
- 記念日代行・サプライズ代行（軸3）は**自社サービスとの競合**なので、そもそも他社を紹介する意味がない。ここは今後もアフィリ対象外

### 入れるときに必ずセットにすること

- **クリック計測を同時に入れる。** 計測設計.md 10章に「爆速開発部で外部リンククリックが測れず詰まった」という失敗が記録されている。リンクを貼ってから計測を後付けする順番にしない
- `rel="sponsored"`（＋必要に応じて `nofollow`）とPR表記を、記事側の手作業ではなく**レンダラー／コンポーネント側で強制する**（同 9章）。書き手が忘れられる形にしない
- 案件データは `記事管理/アフィリ案件マスター.csv` に入れる。style-guide 19-2 のとおり、**提携状況が「提携済」かつトラッキングURLが入っている案件だけ**リンクできる
- 広告を1本でも含む記事は frontmatter に `ad: true`（ステマ規制対応。style-guide 19-5）

## 5. 今は決めないこと

- アフィリエイトをやるかどうか（AdSense とは独立に判断する）
- 広告を入れる記事と入れない記事を分けるか（例：軸1・軸2は広告あり、軸3＝受注直結は広告なし）

判断の材料になるのは記事数とPVの実績なので、**記事管理（`/admin`）と PV（`/admin/stats`）の数字が溜まってから**改めて考える。

## 6. 関連ファイル

- 記事のルール: `.claude/agents/reference/article-style-guide.md`（19章：アフィリエイトリンクと広告表記）
- 計測: `メディア方針/計測設計.md`（9章：クリック計測とPR表記の強制）
- 戦略: `メディア方針/メディア戦略.md`
- 案件データ: `記事管理/アフィリ案件マスター.csv`（現状ヘッダーのみ）
- プライバシーポリシー: `public/privacy.html`（8章に広告配信の記載を追記済み）
- 広告の設定: `src/lib/ads.ts`（パブリッシャーID・スロットIDのSSOT）・`src/components/AdSlot.astro`
- 認定販売者: `public/ads.txt`（**未作成。パブリッシャーID取得後に作る**）
